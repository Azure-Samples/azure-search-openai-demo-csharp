<MudList Clickable="false">
    @foreach (var (title, content) in _supportingContent)
    {
        <MudListItem>
            <MudText Typo="Typo.subtitle2">@title</MudText>
            <MudText Typo="Typo.body1">@((MarkupString)content)</MudText>
        </MudListItem>
    }
</MudList>